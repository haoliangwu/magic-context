# AGENTS.md

## Cloned Dependency Source

Read-only dependency source repositories are available under
`.slim/clonedeps/repos/` for inspection. Do not edit these clones.

- `.slim/clonedeps/repos/xiaohj233__dsh-magic-context/` —
  `xiaohj233/dsh-magic-context` at `v0.1.2`; the original author's dsh plugin
  that `packages/dsh-plugin` was ported from — check it when auditing upstream
  evolution for features our port may be missing.
